<h2>Skills that work toward strengthening your <%= @target %> action</h2>

<ul>
  <% @skills.each do |skill| %>
    <%= link_to skill.name, skill_path(skill.name), :method => "get" %><br>
  <% end %>
</ul>
